package com.servlet_config_servlet_context;
/* SERVLET CONFIG AND SERVLET CONTEXT
 * we use these two to get the initial value for the servlet or application
 *
 * To use it
 * + just go to xml file
 * + do create some connection
 * + then create a tag called context-param
 * + then create two other tag with it
 * - param-name
 * - parm-value
 *
 * YOU SHOULD KNOW THAT SERVLET CONFIG IS FOR EVERYBODY SERVLET IN WEB.XML IS LIKE LOCAL VARIABLE
 * BUT SERVLET CONTEXT WE IS LIKE PRIVATE FOR A CERTAIN SERVLET AND WE NORMALLY CREATE THIS
 * INSIDE THE <servlet>
 *
 *
 * */
